<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The daemon is reached over Tailscale, so this is the whole network need. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- The daemon speaks plain ws over Tailscale or a LAN, never the public
         internet, and there is no certificate to verify. -->
    <application
        android:usesCleartextTraffic="true"
        android:label="nabu"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nabu">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
