package fixture

// Wrap breaks text into lines no longer than width runes, at spaces where it
// can and mid-word where a single word is longer than the line.
func Wrap(text string, width int) []string {
	panic("not implemented")
}
