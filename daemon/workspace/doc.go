// Package workspace resolves the working directory, computes the workspace key (slug
// of the git common dir, else the absolute path), tracks trust, and provides the
// permission plumbing the gate uses.
package workspace
