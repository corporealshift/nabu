package agent

import "context"

// runLoop drives one session's turns. Task 14 implements it; this stub lets
// the lifecycle land and be tested first.
func (m *Manager) runLoop(ctx context.Context, h *sessionHandle) {
	_ = ctx
	_ = h
}
