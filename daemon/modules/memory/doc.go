// Package memory is cross-session memory: markdown files with a MEMORY.md index per
// workspace and globally, injected as a prefix context block, BM25 recall, model-initiated
// saves, and git versioning of the memory directory. The curator pass and consolidation
// are not built yet.
package memory
