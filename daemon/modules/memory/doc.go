// Package memory is cross-session memory: markdown files with a MEMORY.md index per
// workspace and globally, BM25 recall, model-initiated saves, a curator pass at session
// end and before compaction, consolidation, and git versioning of the memory directory.
package memory
