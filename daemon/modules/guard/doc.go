// Package guard is the tool gate policy: allow, deny or ask by command pattern, path
// and risk tier, honouring the session's permission_mode. It is always compiled in;
// disabling it appends a notice.
package guard
