// Package modules is the registration list. All returns every module compiled into
// the daemon, in dispatch order. Adding a module means adding a package under this
// directory and one line to All.
package modules
